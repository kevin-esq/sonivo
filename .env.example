# Optional overrides for compose.yaml (copy to `.env` if needed).
# Development-only credentials — not production secrets.
# Defaults already match appsettings.Development.json when unset.

POSTGRES_DB=sonivo_dev
POSTGRES_USER=sonivo
POSTGRES_PASSWORD=sonivo
POSTGRES_HOST_PORT=5433
